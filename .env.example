# Optional overrides. Git Plus can be started without these values and will
# guide first-run setup in the web UI.
# PASSWORD=your-password
# ENCRYPTION_PASSPHRASE=your-encryption-passphrase
# GIT_PLUS_DATA_DIR=./data
